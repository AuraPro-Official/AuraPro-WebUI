# EPUB Concept Wiki — Persistent Task Status

**Purpose:** Durable implementation state for SDD work. A new session must read
this file and `epub_concept_sdd.md` before making changes.

## Update rules

- Each task has one state: `planned`, `in_progress`, `blocked`, `completed`, or
  `superseded`.
- Update this file in the same change as the implementation or verification that
  moves a task forward. Record evidence (test name, command, or review link).
- Do not mark a task `completed` until its listed acceptance evidence passes.
- Add newly discovered work as a numbered task; do not silently fold it into an
  existing task.
- At the completion of each implementation phase, commit only that phase's
  reviewed files. Do not include pre-existing or unrelated working-tree edits.
- After each completed phase's acceptance evidence passes, push that phase's
  focused commit(s) and open one GitHub Pull Request. Keep subsequent phases on
  a new branch based on the accepted phase, so review remains small and
  independently mergeable.
- For all GitHub mutations, use the authenticated `gh` CLI rather than the
  GitHub App connector. The connector may be used for read-only discovery, but
  it lacks required repository mutation permissions in this project.

## Confirmed product decisions

| ID | Decision | Status |
|---|---|---|
| D-001 | Source content is stable visible EPUB text, not XHTML markup; preserve text and punctuation exactly. | confirmed |
| D-002 | Store complete paragraphs; responses include full content plus verified continuous excerpts and offsets. | confirmed |
| D-003 | Cloud Batch is an administrator-configured, one-time offline process; online inference remains local/private. | confirmed |
| D-004 | All authenticated users can browse and search; administration is the only write/cost-bearing role. | confirmed |
| D-005 | The feature UI lives in AuraPro-WebUI and is displayed by Desktop's WebView. | confirmed |
| D-006 | Normal HTML flow whitespace collapses per visible-text semantics; `<pre>` whitespace is preserved. | confirmed |
| D-007 | First release targets textual EPUBs. Preserve typed text blocks; images and tables are out of scope and warn. | confirmed |
| D-008 | Keep Batch prompt generic initially. Add only passage-relevant seed-term context if review proves the remote model misses established proper nouns; deterministic seed matching remains authoritative. | deferred optimization |
| D-009 | First operational acceptance uses a Simplified Chinese EPUB. Retrieval windows target 800 Unicode code points with 150 overlap and prefer Chinese sentence boundaries; source passages remain intact. | confirmed |
| D-010 | End users install only AuraPro Desktop and WebUI. Desktop owns local llama.cpp/model lifecycle; Homebrew/manual runtime setup is development and E2E-only. | confirmed |
| D-011 | Concept-extraction quality is calibrated locally before any cloud submission: use the Desktop-managed Qwen runtime for prompt/schema/offset iteration on a deterministic cross-chapter sample, then run the same versioned profile against an administrator-authorized OpenAI Batch sample before any full-book Batch. Local results are not treated as evidence of cloud-model quality. | confirmed |
| D-012 | A passage-mention index alone is insufficient. First release must add a grounded concept-relation layer: deterministic TOC structure, an offline relation Batch whose endpoints are existing concepts/TOC nodes, controlled predicates and exact source evidence, then relation-aware graph expansion plus local reranking at query time. Relation identity is global across the shared library; its assertions and evidence remain version-scoped so support can accumulate across books without losing provenance. | confirmed |

## Work breakdown

| ID | Task | State | Depends on | Acceptance evidence / notes |
|---|---|---|---|---|
| T-000 | Baseline audit of existing prototype | completed | — | Existing parser, DB, Batch, search, UI, and tests reviewed. Critical concept-occurrence foreign-key ordering failure reproduced. |
| T-010 | Finalize SDD specification and persistent task process | completed | T-000 | SDD decisions D-001 through D-007 are recorded; parser, storage/search, and UI reviews completed. |
| T-015 | Provision a supported Python test environment | superseded | T-010 | Superseded by T-150. The recorded temporary source build of Python 3.12.13 under `/private/var/folders/.../python-build.20260801153902.35544/` no longer exists, so this task's evidence was not reproducible. Homebrew 3.12/3.14 remain faulty (`xml.parsers.expat` fails on a `libexpat.1.dylib` symbol mismatch). Use `scripts/epub_test_env.sh`. |
| T-020 | EPUB parser and faithful passage fixtures | completed | T-010, T-015 | Archive limits/duplicate-member rejection, OPF spine, NAV/NCX fragment paths, typed visible-text passages, ordered fallback text, and warnings for excluded tables/images are implemented. Python 3.12 acceptance: `test_epub_parser_sdd.py` — 5 tests passed. |
| T-025 | Provision independent storage migration and local vector extension | in_progress | T-010 | `sqlite-vec==0.1.9` is pinned in `pyproject.toml`/`uv.lock`. A healthy temporary validation runtime uses `pysqlite3` compiled against Homebrew SQLite and passes real `vec0` cosine KNN tests. `SQLiteVecDerivedVectorBackend` persists only derived vectors, isolates profile/dimension indexes, prevents source rebinding, and has a connection-scoped SQL health check; startup/admin runtime status degrades explicitly if the extension is unavailable (`test_epub_sqlite_vec_backend.py` — 3 tests passed). SQLite/PG migration runners remain. |
| T-030 | Independent store, migrations, versioning, and source integrity | in_progress | T-010, T-025 | SQLite canonical-store baseline implemented, including immutable passages, verified derived offsets, full-file hash dedupe, FK-safe concepts, and durable Batch job/item records. `/opt/homebrew/bin/python3.14 -m unittest discover -s AuraPro-WebUI/test -p 'test_epub_store.py' -v` passes 5 isolated-store tests; SQLite vector extension and PostgreSQL parity remain. |
| T-040 | Durable Batch job service and glossary-concept ingest | in_progress | T-030 | Provider-neutral SQLite service now persists draft requests before submit, idempotent provider submission/poll/recovery, atomic concept/mention or section-graph ingest, failed-item successor jobs, and credential-field rejection. Administrator history exposes only lifecycle metadata, aggregate item state, and safe per-item summaries—never prompts, model output, raw errors, or credentials. The explicit recovery action polls only durable `SUBMITTED`/`RUNNING` jobs through configured providers and cannot submit a `DRAFT`; unavailable providers are reported as skipped. A durable sample-review gate blocks every full `openai-batch` unless an administrator approved a same-version, same-kind, same-model-profile sample that is `SUCCEEDED` with every item ingested; its identifier-only audit record includes the version, job kind, sample job, reviewer, decision, and review time. `OpenAIBatchProvider` uploads validated OpenAI JSONL, uses idempotency headers, normalizes lifecycle states, and translates output/error JSONL without persisting credentials. Python 3.12: `test_epub_batch.py` — 16 tests passed. PostgreSQL adapter remains. |
| T-050 | Local-only inference adapters and derived vector index | in_progress | T-030 | EPUB now consumes AuraPro's existing local RAG embedding and reranking functions via fail-closed adapters; embedding bridges from worker thread to AuraPro's application event loop, while the Cross-Encoder receives document-compatible immutable strings. Runtime validates that generic RAG is in-process or the selected Ollama URL is private/explicitly trusted before EPUB can call it, and reports policy/model health without leaking configuration. The dedicated llama.cpp Tier-2 adapter checks its private `/health`, uses the local OpenAI-compatible completion endpoint, strictly accepts only existing candidate concepts, and has an async worker-thread facade. Python 3.12: `test_epub_local_inference.py` — 16 tests passed; `test_epub_runtime.py` — 5 tests passed. PostgreSQL/pgvector remains. |
| T-060 | Search pipeline, exact excerpt contract, and pagination | completed | T-020, T-030, T-050 | Dependency-free local-only search orchestration now uses a trie multi-pattern Tier-1 matcher (Latin boundaries/CJK direct phrase), explicit degraded Tier-2 resolver, exhaustive graph count/offset pagination, verified full-passage excerpts, derived-vector recall, cross-encoder reranking, and MMR. Python 3.12: `test_epub_search.py` — 5 tests passed; store read-surface coverage is included in `test_epub_store.py` — 6 tests passed. |
| T-070 | Authenticated REST API and admin authorization | completed | T-040, T-060 | Python 3.12 `test_epub_api.py` — 5 tests passed: authenticated shared reads/search, admin-only mutations, fail-closed missing startup service, and an initialized independent runtime serving the real router. The test isolates only the auth dependency identity, while exercising the real FastAPI router and service. |
| T-080 | AuraPro-WebUI frontend and Desktop WebView integration | completed | T-070 | Typed `/api/v1/epub` client plus `/epub` verified-user browse/search and `/admin/epub` admin workflow are implemented. The shared EPUB library is discoverable from the WebUI sidebar and the admin layout exposes the admin route; no Desktop-specific code was added. Node 22 Vitest client suite — 3 tests passed. Full-project strict checking still reports pre-existing errors, while the new EPUB page is clean. |
| T-090 | End-to-end, migration, resilience, and operational validation | in_progress | T-020, T-040, T-060, T-070, T-080 | Component acceptance suite passes: parser 5, store 6, Batch 10, local inference 16, search 5, API 5, frontend client 3. Real local E2E on 2026-08-02 imported a user-supplied Simplified Chinese EPUB: 1,224 source passages / 407,037 visible characters yielded 1,436 units, all indexed `READY` with 0 degraded/failed; every stored passage field matched a fresh parser result and every unit's offsets reconstructed the exact source substring. Local vector recall + Cross-Encoder + MMR returned faithful results for a verified source term; llama.cpp resolved a semantic paraphrase to an existing test concept without degradation. The actual Desktop-managed Python initially lacked locked `sqlite-vec==0.1.9`; it was installed locally only for E2E, so T-110 must package it. A normal Desktop shutdown removed its runtime descriptor, but an interrupt-driven test termination left a stale descriptor after its child server stopped; WebUI health checks fail closed, and T-090 must cover stale-descriptor cleanup/recovery. PostgreSQL/pgvector parity, remote Batch execution with administrator credentials, and resilience scenarios remain. |
| T-095 | Provision a healthy native SQLite extension test runtime | completed | T-025 | Homebrew Python 3.14 remains broken (`pyexpat` symbol mismatch), but a temporary Python 3.12 test runtime with `pysqlite3` compiled against Homebrew SQLite successfully loads locked `sqlite-vec==0.1.9` and executes a real `vec0` cosine KNN query. |
| T-100 | Automatically derive Chinese-first retrieval units on EPUB import | completed | T-030, D-009 | Import now persists exact source windows before a version becomes `READY`: <=800 code points yields one whole-passage unit; longer text targets 800 with 150 overlap and prefers `。！？；`, then `.!?;`, before a character fallback. Re-running derivation reuses the exact stored units. Compatible Python 3.12: `test_epub_retrieval_units.py` — 7 tests passed; `test_epub_store.py` — 6 regression tests passed. |
| T-105 | Bulk derived-vector indexing and rebuild workflow | completed | T-025, T-050, T-080, T-100 | Admins can index all non-ready derived windows or explicitly rebuild a selected EPUB version, with stable source ordering, persisted per-unit READY/FAILED outcomes, degraded-local-runtime preservation, bounded error details, and no need to expose retrieval-unit IDs. Compatible Python 3.12: `test_epub_retrieval_units.py` — 10 tests passed; `test_epub_store.py` — 7 tests passed; `test_epub_api.py` — 5 tests passed. Node 22: `src/lib/apis/epub/index.test.ts` — 4 tests passed. |
| T-110 | Desktop-managed local runtime packaging | completed | T-050, T-090, D-010 | Desktop now provisions locked `sqlite-vec==0.1.9`, manages the approved local GGUF through its resumable download flow, and atomically publishes/removes a 0600 loopback runtime descriptor during normal lifecycle shutdown. WebUI consumes only its absolute `AURAPRO_DESKTOP_LLM_RUNTIME_FILE` path, re-reads endpoint/model changes per resolver operation, and fails closed without falling back to static settings. Compatible Python 3.12 EPUB suite: 74 tests passed. On macOS arm64, Desktop's required Node 24.18.1/npm 12.0.1 typecheck passed with 0 TypeScript/Svelte diagnostics and production build passed; an isolated fresh Desktop installation provisioned Python, llama.cpp, and the local model without Homebrew, published a 0600 descriptor to a healthy loopback service, and removed it on normal shutdown. A live WebUI resolver then read the descriptor and completed a synthetic candidate resolution. Cross-platform release/package compatibility remains a T-090 release-acceptance activity. |
| T-115 | Local-first concept prompt calibration | in_progress | T-040, T-110, D-011 | Implemented versioned `zh-glossary-v1`/`v2`/`v3` profiles, deterministic chapter-stratified selection, Desktop Qwen runner, local-only unique-evidence offset derivation, strict validation, admin API/UI, and content-free reports. Compatible Python 3.12 EPUB suite: 80 tests passed. On the retained local Qwen runtime, v1 produced malformed/non-object JSON on a complex Chinese passage; v2 returned bounded JSON but omitted required mentions; v3 returned mentions but only one could be uniquely verified against the immutable source. Decision: local Qwen validates transport/format/evidence-contract iteration, not remote semantic quality; do not weaken the remote candidate profile merely to fit the 3B model. The first authorized cloud sample must pin a model snapshot and reuse v3 plus deterministic selection. **Correction (T-155):** this has since happened — four administrator-authorized cloud samples exist against pinned `gpt-4.1-2025-04-14` and `gpt-4o-mini-2024-07-18` snapshots, two on v3 and two on v4. Their evaluation is tracked as T-155, not here. |
| T-120 | Grounded concept-relation graph and relation-aware retrieval | completed | T-040, T-060, T-115, D-012 | Deterministic TOC provenance, durable evidence-backed global relations with version-scoped assertions, section-graph Batch, administrator review, bounded `HAS_PART` traversal, and local Cross-Encoder/MMR graph/vector fusion are implemented. Remote Batch acceptance remains a separate administrator-authorized T-090 activity. |
| T-120a | Relation persistence and bounded graph expansion | completed | T-120 | Global relation identities retain separately version-scoped assertions and exact source evidence. Both endpoints must already have a mention in the assertion's EPUB version; invalid source evidence is rejected atomically. Search follows non-rejected `HAS_PART` edges to depth 2 and marks expanded results in provenance, while still rendering only verified source passages/excerpts. |
| T-120b | TOC-scoped relation Batch, review, and rank fusion | completed | T-120a, T-115 | `SECTION_GRAPH` jobs use deterministic bounded TOC packets and strict packet-local concepts, exact multi-passage mentions, and controlled evidence-backed relations. Per-item ingest is atomic. Admins can review version-scoped assertions. `fused_results` combines graph and vector candidates with only local Cross-Encoder/MMR and fails closed on a local model error while retaining the compatible independent channels. |
| T-125 | Enforce reviewed cloud-sample quality gate | completed | T-040, T-115 | SQLite persists identifier-only sample review decisions. A full `openai-batch` is rejected unless an administrator approved a same-version, same-kind, same-model-profile sample whose job and every item reached `SUCCEEDED`; a concept sample or a different model snapshot cannot unlock another job. Admin-only API/UI exposes review/list actions without prompts, model output, source text, or credentials. Compatible Python 3.12: Batch 16/16, API 9/9, full EPUB suite 94/94; frontend API-client suite 7/7. |
| T-130 | Safe terminal Batch reconciliation and credit-recovery retry | completed | T-040 | A terminal `FAILED`/`CANCELLED` provider job reconciles missing items only after its complete output/error stream has been fetched and structurally validated. Explicit successes remain durable, while conclusively unreported items become controlled `FAILED` retry candidates; successor jobs never copy `SUCCEEDED` items. Output retrieval/download/parsing failures preserve all unconfirmed items under the safe `RESULTS_PENDING_RETRIEVAL` operator state; an administrator must poll again before retry is enabled. Admin history/UI exposes only this boolean state and counts, not raw provider errors, prompts, outputs, source text, or credentials. Compatible Python 3.12: `test_epub_batch.py` — 19 tests passed. |
| T-135 | Ground cloud Batch evidence offsets without weakening source fidelity | completed | T-115, T-130 | `zh-glossary-v4` adds bounded adjacent evidence anchors. Cloud ingest may derive invalid model offsets only from a unique literal or a uniquely anchored repeated literal; unresolved output remains a failed item. A later poll can safely ingest a formerly failed item, while a different output cannot replace a success. Compatible Python 3.12: full `test_epub_*.py` suite — 101 tests passed. |
| T-150 | Reproducible local Python test runtime | completed | T-015 | `scripts/epub_test_env.sh` provisions a durable minimal venv from a python-build-standalone CPython 3.12 — the same distribution and release asset AuraPro Desktop ships to end users (`generateDownloadUrl` in `AuraPro-Desktop/src/main/utils/index.ts`) — and hard-validates both `import xml.parsers.expat` and `sqlite3.Connection.enable_load_extension` before use. It installs only `typer`, `uvicorn`, `fastapi`, `httpx`, `python-multipart`, `sqlite-vec`, and `ruff`, reading its pins from `pyproject.toml`. `test_epub_sqlite_vec_backend.py` now uses the stdlib `sqlite3` module that `load_sqlite_vec` actually uses in production instead of a second `pysqlite3` binding, and skips rather than errors where loadable extensions are unavailable. Baseline before: 88 tests with 4 collection errors. After: 101 tests, 0 collection errors across all 14 modules. |
| T-140 | Fail-closed error paths must not raise `NameError` | completed | T-150 | Two exception types were referenced but never imported, so the paths that exist to degrade gracefully crashed instead. `retrieval/epub/sqlite_vec_backend.py` used `SQLiteVecUnavailable` in `healthcheck()` and in `_ensure_loaded`'s cached-connection branch; because `epub_runtime._runtime_status` catches only that type, a connection that lost sqlite-vec surfaced as an opaque 503 rather than the degraded vector-subsystem payload SDD 3.1 requires. `services/epub_concept.py` used `BatchServiceError`/`BatchPayloadError` in five separate handlers, so a Batch error escaped untranslated and administrators saw HTTP 500 instead of 400/404. Neither path had coverage: names used only in `except` clauses are invisible to a passing suite. Regression tests reproduce `NameError` on both when the fix is backed out, and cover all five Batch handlers individually. `ruff check --select F` over the EPUB modules reports `All checks passed!` (10 `F821` findings with the fixes reverted) and ruff now ships in the test runtime. Suite: 105 tests passed. |
| T-145 | Retire the unauthenticated legacy EPUB prototype | completed | T-140 | `routers/epub_concept.py` was registered in `main.py` with no authorization on any of its seven routes; `POST /parse_epub` accepted an unauthenticated EPUB upload into a second, older SQLite file and `/search` read it back — a duplicate surface violating SDD 6. The cluster was self-contained (router, `utils/concept_wiki.py`, `utils/batch_pipeline.py`, `utils/epub_concept_db.py`, the `retrieval/loaders/epub_parser.py` shim, `test/test_epub_concept.py`) and no frontend code called it; all removed, along with the `EPUBParser.parse()` compatibility adapter whose only caller was the router. Desktop's untracked, unreferenced `EpubConceptSearch.svelte` is deleted (SDD 1 and 6: Desktop is not a separate feature client); Desktop's legitimate runtime provisioning is untouched. `epub_concept_search_spec.md` storage and code-layout sections rewritten to the real architecture with a note recording their subordination to the SDD. Verified: 103 tests passed, `ruff --select F` clean, zero dangling references, `main.py` parses. The orphaned `epub_concept.db` files are local data, not code, and were left in place. |
| T-155 | Tune the OpenAI Batch prompt against the existing cloud samples | in_progress | T-140, T-145, T-125 | All four samples are now terminal and diagnosed. Content-free per-item diagnostics (reason slug plus counts and flags, whitelisted on write and re-validated on read, no offsets and no text) landed before polling, so the v4 failure detail survived. First poll: v4 did not beat v3 — gpt-4.1 13/20 both, gpt-4o-mini 17/20 on v3 but 9/20 on v4. Diagnostics showed all 12 `EVIDENCE_AMBIGUOUS` failures had `occurrence_count = 1` and `anchored_candidate_count = 0`: unique evidence vetoed by a wrong model anchor, with `direct_is_exact = 0` leaving no other route. `_ground_openai_concept_payload` applied the anchor filter to every v4-shaped mention, while SDD 4.2.1 scopes it to repeated evidence and separately sanctions unique-literal repair; the sibling `ANCHOR_MISSING` check was already correctly scoped. Restricting the filter to `occurrence_count > 1` restored that and recovered exactly the predicted 12 items on a free re-poll: **gpt-4.1 19/20, gpt-4o-mini 15/20**; graph grew 42→52 concepts and 43→71 mentions. The direct-exact-offset branch still verifies anchors, a documented deliberate asymmetry. Every remaining failure now has one evidenced cause: 6 `PROVIDER_ITEM_ERROR` non-JSON truncations on 692–1174 code-point passages against `max_tokens=512`; v3's 7 `EVIDENCE_AMBIGUOUS` are genuine repeats (occurrence 2–38, evidence 1–6 code points) that anchorless v3 cannot resolve; v3's 3 `EVIDENCE_ABSENT` are paraphrases (occurrence 0, evidence 9–28 code points). `zh-glossary-v5` targets each: `max_tokens` 512→2048, anchors mandatory-empty for unique evidence and required only for repeats, and a verbatim-copy clause. v1–v4 are byte-identical and snapshot-tested because approvals key on the profile; schema selection moved to an explicit `uses_context_anchors` flag so promoting v5 could not silently demote v4 and break replay. Suite: 123 tests passed; ruff `--select F` clean; frontend client 8 tests passed. **Remaining:** no v5 cloud sample has been submitted — that is new spend and requires explicit administrator authorization. No sample of any profile has been approved, so a full `openai-batch` remains durably blocked. |
| T-160 | Administrator concept merge | completed | T-155 | Ingest correctly refuses a suggestion matching two concepts and demands review (SDD 4.2), but no administrator action could resolve one: the router carried a single concept route, upsert, with no list, merge, alias removal, or delete, and `REJECTED` does not help because the resolver's lookup ignores status. That gap blocked the only approvable sample. `SQLiteEpubStore.merge_concepts` folds a source into a target in one transaction: the source's canonical spelling becomes an alias, since that is what a future model response matches on; mentions keep passage, offsets, evidence, and source with only `concept_id` repointed, and one duplicating an existing span is dropped; a relation between the merged pair would become a self-loop the schema forbids by CHECK, so it is deleted and counted; a relation whose repointed endpoints duplicate an existing one is folded into it, moving version-scoped assertions and deduplicating evidence, so no grounded span is discarded. Two schema subtleties are covered by tests: `normalized_alias` is globally unique rather than per concept, so a spelling already owned by a third concept is refused rather than silently dropped; and the mention unique constraint does not deduplicate offset-less rows because SQLite treats NULLs as distinct. A test walks `PRAGMA foreign_key_list` over every table so a later migration referencing `concepts` fails loudly instead of orphaning rows. `_MIGRATION_5` adds `concept_merges`; `SCHEMA_VERSION` 4→5. Admin-only `GET /admin/concepts` and `POST /admin/concepts/merge` land with typed client functions. Suite: 142 tests passed; ruff `--select F` clean; frontend client 9 tests passed. |
| T-165 | Ground section-graph spans and add `zh-section-graph-v2` | completed | T-155, T-120b | `SECTION_GRAPH` had no offset repair at all: `zh-section-graph-v1` demanded start/end on every mention and relation evidence span with no anchors, and `_ingest_section_graph` wrote through `_add_mentions`, which hard-rejects any offset whose slice differs from the evidence. Measured across four cloud samples, a model supplies a correct code-point offset about 1 time in 37, and per-item ingest is atomic over a packet holding many spans, so P(packet succeeds) was approximately 0.027^n — a submitted batch would have yielded essentially nothing. It went unnoticed because every section-graph test constructed correct offsets by hand. `_resolve_evidence_span` is now the single span resolver for both ingest paths (`e4e224b`, behaviour-neutral at 142 tests); the concept path's precedence is preserved exactly — anchors disambiguate only a repeated literal, and a supplied-and-exact offset still has its anchors verified. Section-graph shape is detected per span from the returned field set rather than per payload, so a stored v1 request still replays and neither shape's offsets are trusted. Ingest splits into a read-only grounding pass over the whole packet and a write of the already-grounded result, making atomicity structural rather than rollback-dependent. `zh-section-graph-v2` drops offsets from the contract, asks only for `passage_id` and evidence with anchors required only for a repeated literal, and carries over the two v6 levers that worked (verbatim copy naming the normalizations to avoid; minimum evidence span with a short-passage escape hatch, which also makes a literal likelier to be unique). `max_tokens` 1800→8192 against measured packets averaging 9,465 characters, and the instruction now caps concepts and relations so the worst case is finite. v1 stays registered and selectable, verified byte-identical by comparing its emitted request digest against HEAD. New content-free slugs: `LOCAL_ID_INVALID`, `RELATION_ENDPOINT_UNRESOLVED`, `RELATION_REJECTED`. Every section-graph test now drives absent or deliberately wrong offsets and asserts the stored span is a byte-exact source slice. Suite: 156 tests passed; ruff `--select F` clean; frontend client 9 tests passed. |
| T-170a | Portable analysis overlay: export, import, reattach by content hash | completed | T-155, T-160, T-165 | The concept graph existed only where it was built, so every user would have had to pay for a Batch run producing an identical result. The overlay exports concepts, aliases, definitions, mentions and relations keyed by `epub_sha256`, and reattaches to an independently parsed copy of the same EPUB. It transports **no passage text**: mentions and relation evidence travel as `(ordinal, content_sha256, start, end)` and the importer derives the evidence string from the reader's own passage. Five ordered gates — EPUB hash, `PARSER_FORMAT_VERSION`, passage fingerprint digest, per-span `content_sha256`, offset bounds — make it impossible to store anything not already in the reader's own book. Unknown fields are rejected at every level, since an extra `evidence` key on a mention is exactly where text would be smuggled. Apply is strictly additive and local decisions win: `APPROVED` is never downgraded, `REJECTED` never resurrected, local spelling/definition survive with published ones demoted to aliases, everything written as `MODEL` source so published output cannot masquerade as an operator's `ADMIN` decision, and a published spelling owned by a different local concept refuses the artifact rather than guessing at a merge. No migration needed (`SCHEMA_VERSION` stays 5). **Real end-to-end on the live book:** exported version `b7e4d0ed` to a 53,506-byte artifact from a 28 MB store, imported the same 1.24 MB EPUB into a fresh store which parsed it independently to a different version id (`218630c4`), applied the overlay — 323 applied / 0 skipped / 0 rejected — and got an identical 83 concepts / 183 mentions / 28 relations / 29 evidence spans, with **183/183 mentions byte-exact against the reader's own passages** and the reader's re-export **byte-identical** to the source export. Text-leak audit on the real artifact: 1 of 56,634 twelve-code-point book windows appears, via a concept label; the invariant test now also bounds contiguous source runs at 40 code points so a future profile letting a definition quote a sentence fails loudly. Suite: 177 tests passed; ruff `--select F` clean; frontend client 12 tests passed. |
| T-175 | Enumerate distinct source spans in the graph channel | completed | T-170a | Real queries showed Channel A returning the same source text several times: `值守员锚站` returned the identical span `[10,17)` twice and `[12,17)` twice, `潮汐源的权重` three nested variants of one sentence. Cause: the count and list worked per mention row, so two concepts anchored on the same or overlapping characters each produced a result. Channel A now enumerates distinct `(passage, start, end)` spans. Exact duplicates collapse by `GROUP BY`; a span strictly contained by another collapses into it via a correlated `NOT EXISTS` whose strictness guard stops identical spans annihilating each other; every absorbed concept stays attributed on the survivor. Partial overlaps are untouched — widening a span to a union would render a citation no concept anchored. Count and list interpolate **one shared SQL fragment**, so they cannot drift; a page-walk test asserts `len(walked) == graph_total` rather than recomputing an expectation. Containment is scoped within a passage, so passage-level recall is provably unchanged. Relation provenance takes the minimum depth across a span's concepts, so a span a directly matched concept anchored stays a direct hit. SDD 4.3 updated to state the new unit of enumeration. **Measured on the live graph:** `graph_total` 22→6, 14→8, 6→1, 14→6 with zero duplicate spans and page-walk equal to the total on every query; citations became substantive, e.g. one hit naming `值守员`+`锚站` instead of five fragments. Suite: 181 tests passed; ruff `--select F` clean. |
| T-180 | Purge sub-floor legacy mentions | planned | T-155 | 54 of 183 stored mentions (30%) fall below the 10-code-point floor `zh-glossary-v6` introduced, including three 1-character `枢` and several 2-character `扰动源`/`锚站`/`汛情`. They were written by v3/v4/v5 before the floor existed, and they are why a `汛期观测` query surfaced `前言` → `枢` as a graph citation. v6 fixed generation; nothing cleaned up what earlier profiles already wrote. The full-book v6 run is expected to replace them, so the open decision is whether to additionally purge or down-rank existing sub-floor mentions rather than wait. Distribution of evidence lengths at time of writing: 1–3 cp ×20, 4–9 cp ×34, 10–29 cp ×82, 30+ cp ×47. **Measured after the full v6 run (2026-08-07):** sub-floor mentions fell from 30% to **13.2% (345 of 2,619)** but did not disappear, because the >=10 code-point floor is only ever *asked for* in the prompt — nothing verifies it at ingest, so a model that ignores it is silently obeyed. The product owner's decision is to **reject** a sub-floor mention at ingest, honouring the short-passage escape hatch, enforced per prompt profile so v1–v5 replay is unaffected. |
| T-185 | Decide graph-panel ordering in the UI | planned | T-175 | Channel A is exhaustive-and-paginated by design rather than ranked, so `graph_limit=5` against `graph_total=22` returns an arbitrary five — which is how `前言` → `枢` reached the top of a `汛期观测` query's graph panel while `fused_results` ranked the same query correctly. Not a defect in the search contract; a question about what the standalone graph panel in `src/routes/(app)/epub/+page.svelte` should show. Options: rank it, order it by book position and say so, or de-emphasise it in favour of fused. |
| T-190 | Bind the approval gate to the prompt profile | completed | T-125, T-200 | SDD 4.2.1 says an approval "for one job kind or model profile cannot unlock another", but `batch_jobs` had no `prompt_profile` column — the profile lived only inside `request_json` — so the gate matched version, job kind and *model* snapshot and nothing else. Theoretical until `zh-glossary-v7` became the default, at which point a full run created without an explicit `prompt_profile` would have ridden the v6 approval while sending v7. `_MIGRATION_6` adds the column (`SCHEMA_VERSION` 5→6); `create_draft` records it for both job kinds, threaded explicitly from the service rather than re-derived, and `create_retry_child` copies the parent's since a retry replays the parent's stored requests. The gate now requires equality **and** refuses NULL on both sides — the requested profile before the query runs, the stored one via an explicit `IS NOT NULL` — because "unknown profile silently matches" is the exact failure being fixed; all four refusal paths share one message so a refusal is not a probe for what is approved. Without a backfill the existing v6 approval would have stopped working, so a service-level backfill derives the profile from each job's stored system instruction by **exact** equality against registered profiles, leaving NULL with a content-free reason on no match or ambiguity, and writing only `WHERE prompt_profile IS NULL` so it can never re-point an approval. `store.py` and the migration import no extraction-policy module. **Verified on a copy of the live store:** before backfill both v6 and v7 refused; backfill resolved 11 of 11 jobs (v3/v4/v6 and the section-graph profiles) with none unknown; after, v6 allowed, v7 refused, unspecified refused; the in-flight 1,224-item job stayed `SUBMITTED` with byte-identical JSONL. SDD 4.2.1 updated. Suite: 203 tests passed; ruff clean; frontend client 13 tests passed. |
| T-195 | Scope section-graph evidence to passage content (`zh-section-graph-v3`) | completed | T-165 | Both section-graph samples failed the approval gate on `EVIDENCE_ABSENT`, which reads like hallucination. Re-fetching provider output and classifying every failing span showed otherwise: the packet sent each passage with a `toc_path` beside its `content` and never scoped what was quotable, so the model cited **section titles** as evidence — `枢纽是全网时基的来源（二）`, `枢纽为测点预备的日常配额`, `主控台的校准法`. **All 6 of gpt-4.1's failing spans were this and none were hallucinated**; gpt-4o-mini had 3 of 9 plus genuine invention. Per failed item: gpt-4.1's 4 failures are 3 `toc_path` and 1 alias-collision review hold, so the gate should clear. v2 already carried a verbatim-copy clause, which is why it did not help — the model was not paraphrasing but quoting a field we handed it unscoped. The fix removes the surface rather than forbidding it: per-passage `toc_path` is no longer emitted at all (packets are TOC-scoped, the packet carries its own path, and SDD 4.2.2 point 1 makes structural provenance deterministic server-side), while the packet-level path stays because point 2 justifies the packet shape by giving one request enough context to recognise a section-level concept with its parts. v3 adds the scoping clause. Verified on the built request: the leaf title is absent from the payload entirely. New content-free slug `EVIDENCE_FROM_TOC_PATH` (exact title equality only) so this class is never again hidden under `EVIDENCE_ABSENT`. v1 and v2 byte-identical and digest-pinned. Suite: 186 tests passed; ruff clean. **Sample `965c2c11` submitted 2026-08-06** (gpt-4.1, 16 packets, ~$0.07); gpt-4o-mini not resubmitted since its hallucinated spans are out of scope here. **Outcome:** v3 scored 15/16 with `EVIDENCE_ABSENT` at 0, confirming the diagnosis. Four administrator merges cleared the alias holds; the last item was a self-relation created by an earlier correct merge, resolved by the SDD 4.2.2 point 6 skip rather than by more merging. Final **16/16, approved, and the full 43-packet run is submitted** as `31efbf3b`. |
| T-200 | Drop offsets from the concept contract (`zh-glossary-v7`) | completed | T-155, T-165 | The 1-in-37 offset-accuracy measurement came from the concept samples, but the conclusion was applied only to `zh-section-graph-v2`. v7 is v6 with the offset fields removed from the mention contract and the shape example brought into line; the minimum evidence span, conditional anchors and verbatim-copy clause carry over, and a test asserts undoing exactly those two edits reproduces v6. `max_tokens` stays 2048 deliberately — it is a generation ceiling, not a spend, so lowering it banks nothing while moving the worst case toward a truncated non-JSON response, and holding it keeps v7 one-variable against the approved v6 sample. Ingest recognises three mention shapes from the returned field set rather than the current default, so v1–v6 replay: verified by rebuilding every superseded profile's request, remote and local, and diffing byte for byte against HEAD. `ANCHOR_MISMATCH` is unreachable for v7 by construction. v6 is now digest-pinned, carrying the one approved sample and the in-flight full-book job. Also fixed an adjacent break: `validate_concept_payload`, used by local calibration, knew only the older shapes and would have scored the new default 0% schema-valid. Suite: 195 tests passed; ruff clean; frontend client 12 tests passed. |

## Phase delivery

| Phase | Commit | Pull request | State / evidence |
|---|---|---|---|
| Parser baseline (T-010/T-020) | `796c763` | pending | Branch `feat/epub-concept-wiki` is ready locally. Push to `origin` was rejected because SSH identity `mikdw` lacks write permission to `AuraPro-Official/AuraPro-WebUI`; create the PR after repository write access is granted. |
| Source store and vector boundary (T-025/T-030 baseline) | `6a6f1d2` | pending | Isolated store tests 5/5 and sqlite-vec boundary tests pass. Push remains pending repository administrator approval. |
| Durable OpenAI Batch workflow (T-040 baseline) | current branch head | pending | Batch lifecycle and mocked OpenAI provider tests 10/10 pass; no real API key or network call. Push remains pending repository administrator approval. |
| Local inference and faithful search (T-050/T-060) | `a5b4071`, `d257704` | pending | Local-only inference/vector boundary tests 8/8; search contract tests 5/5. Push remains pending repository administrator approval. |
| Authenticated API (T-070) | `78854f3`, `526caa6` | pending | FastAPI authorization/service suite 5/5 passes, including initialized-runtime route availability. Push remains pending repository administrator approval. |
| WebUI workflow (T-080) | `abff971`, `363e226` | pending | Vitest client suite 3/3 passes; new EPUB page has no strict-check findings. Push remains pending repository administrator approval. |
| Persistent local vector backend (T-025/T-050) | pending commit | pending | Real sqlite-vec backend KNN, source-identity, and active-connection health checks pass 3/3 in the healthy temporary extension runtime; runtime status is fail-closed on degraded vector/model health. |
| Automatic retrieval-unit derivation (T-100) | pending commit | pending | Chinese-window planning, source fidelity, import ordering, and retry idempotency pass in 7 focused tests. |
| Desktop runtime handoff (T-110) | WebUI `d98bd80`; Desktop `3bb52fa` | [WebUI #8](https://github.com/AuraPro-Official/AuraPro-WebUI/pull/8); [Desktop #11](https://github.com/AuraPro-Official/AuraPro-Desktop/pull/11) | Both PRs are ready for review. WebUI descriptor unit/runtime tests plus full compatible Python EPUB suite pass (74 tests); Desktop managed Python loads `sqlite-vec v0.1.9`; required Node 24.18.1/npm 12.0.1 typecheck and production build pass on macOS arm64. A clean isolated Desktop start self-provisioned its runtime without Homebrew; descriptor permissions, live WebUI handoff, and shutdown cleanup were verified. Cross-platform release/package compatibility remains T-090 acceptance work. |
| Local prompt-calibration workflow (T-115) | `7b8b168` | [WebUI #9](https://github.com/AuraPro-Official/AuraPro-WebUI/pull/9) | Versioned profile requests use deterministic decoding and remote Structured Outputs; Desktop Qwen calibration exposes only aggregate/item validation metadata. Compatible Python 3.12 EPUB suite: 80 tests passed. Local calibration established that the 3B model is useful for contract testing but is not a sufficient remote-quality proxy. The statement "no cloud request has been created" was true when written and is now superseded: see T-155. |
| Grounded relation graph (T-120a) | `82dbf2b`, `3118fc3` | [WebUI #10](https://github.com/AuraPro-Official/AuraPro-WebUI/pull/10) | Global relation identities accumulate separately version-scoped, exact immutable-source evidence assertions. Each assertion requires existing in-version concept endpoints. Bounded `HAS_PART` traversal adds related sections to graph results with explicit provenance; compatible Python 3.12 EPUB suite: 82 tests passed. |
| Batch failure diagnostics and grounding precedence (T-155 part 1) | `8938003`, `7fe229c` | [WebUI #17](https://github.com/AuraPro-Official/AuraPro-WebUI/pull/17) | Branch `feat/epub-batch-failure-diagnostics`, based on `feat/epub-hardening-legacy-retirement`. Content-free per-item diagnostics landed before any poll, which is what made the root cause visible: all 12 v4 ambiguity failures were unique evidence vetoed by a wrong anchor, not genuine ambiguity. Restricting the anchor filter to repeated evidence recovered them at no provider cost — gpt-4.1 13→19/20, gpt-4o-mini 9→15/20. `zh-glossary-v5` is authored and unit-tested but has no cloud sample. Suite: 123 tests passed; ruff `--select F` clean; frontend client 8 tests passed. |
| Hardening and legacy retirement (T-150/T-140/T-145) | `551fb78`, `6ff5aba`, `c4b4150` | [WebUI #16](https://github.com/AuraPro-Official/AuraPro-WebUI/pull/16) | Branch `feat/epub-hardening-legacy-retirement`, based on `feat/epub-cloud-evidence-grounding` (WebUI PR #15). `scripts/epub_test_env.sh` makes the suite reproducibly runnable for the first time since T-015's interpreter was lost; both `NameError` fail-closed defects are fixed with regression tests that reproduce the crash when backed out; the unauthenticated `/api/v1/epub_concept` surface and its legacy store, batch, and parser-shim modules are deleted. Final state: 103 tests passed, `ruff check --select F` clean over all EPUB modules, zero dangling references. Desktop's `EpubConceptSearch.svelte` was untracked, so its removal needs no Desktop commit. |
| TOC section-graph Batch, review, and fusion (T-120b) | pending commit | pending | `SECTION_GRAPH` jobs carry bounded TOC packets and strict structured outputs. One item atomically resolves concepts, exact mentions across packet passages, and relation assertions; admin-only draft and version-scoped relation approval/rejection surfaces are covered. `fused_results` applies private Cross-Encoder/MMR to both graph and vector candidates. Compatible Python 3.12 EPUB suite: 89 tests passed. |

## Active-session handoff

- **Current focus:** T-155 cloud prompt tuning, then T-090 production/migration/resilience acceptance. T-120, and the T-150/T-140/T-145 hardening phase, are complete.
- **Test runtime:** run `bash scripts/epub_test_env.sh` once, then
  `"$HOME/.cache/aurapro/epub-test-venv/bin/python" -m unittest discover -s test -p 'test_epub_*.py'`.
  The same venv carries ruff for the `--select F` error-path guard. Do not rely on Homebrew or
  pyenv CPython: the first fails `xml.parsers.expat`, the second lacks loadable SQLite extensions.
- **Current implementation state:** The frontend keeps reads on authenticated `/epub` routes and renders complete passages plus explicit exact excerpts. The administrator route is separate, presents imports, Batch controls/history, sample approval, concept review, and derived-index operations, and sends no provider credentials. Backend authorization remains authoritative: full OpenAI Batch creation is durably blocked until a fully ingested, same-version, same-kind, same-model-profile cloud sample has an administrator approval record. Import automatically writes retry-safe, Chinese-first derived retrieval windows without changing canonical passages. Local startup attaches a persistent independent SQLite service, and the tested sqlite-vec backend is available for production adapter wiring; the remote PostgreSQL implementation remains.
- **Retrieval quality validated end-to-end on sample data (2026-08-06).** The real pipeline was run
  against the live store (85 concepts / 183 mentions / 28 relations / 1,442 indexed units) using the
  Desktop runtime's model stack at `~/Library/Application Support/aurapro/python` — the only
  environment on this machine carrying torch and sentence-transformers; the T-150 test venv
  deliberately does not. Embeddings `BAAI/bge-small-zh-v1.5` (512), reranker
  `BAAI/bge-reranker-base`, Tier-2 disabled. Findings: vector recall is strong and topically
  correct across seven Chinese questions; **every excerpt in every channel verified byte-exact**;
  no degraded state; Tier-1 resolved multi-concept queries without needing Tier-2; and relation
  expansion demonstrably contributes — `潮汐源的权重` produced 7 relation-derived hits of 22
  (`relation:HAS_PART:1` ×4, `:2` ×3) reaching `潮汐源权重` → `春汛、夏汛、秋汛、冬汛` and
  `调度工作`, citations a direct term match could not have found. Three defects found and recorded
  as T-175, T-180, T-185. Note `resolved_concepts` reports Tier-1 matches only, so relation
  expansion is invisible there and must be read from hit provenance.
- **Full concept run after administrator merge review: 1,167 of 1,224.** The 58 alias-collision holds were reviewed as 30 distinct colliding groups and classified: orthographic/honorific variants merged (`主控制台`←`主控台`, `锚点记`←`《锚点记》`, `双色标`←`色标`, `枢纽的调度计划`←`调度计划`/`枢调度计划`, and others); distinct designations for one referent merged into `全域潮汐枢纽` (`潮汐源`, `潮汐的源`, `潮位站`, `主枢`, `全域潮汐枢纽自身`, ~360 mentions) and `甲组`←`乙班的甲组`; specialization pairs and genuine different-concept collisions **declined** (`枢纽的统辖` vs `枢纽的权重`; the three-way `统辖`/`权重`/`潮汐源`; section titles extracted as concepts). The bare-vs-`枢`-prefixed family was decided **empirically rather than by intuition** — the text states 「除了潮汐源之外，没有任何站点与『权重』这个词相关」 and 「『基准』这个词只用于枢纽才恰当」, so those bare terms never denote anything but the hub's and were merged; this reversed the reviewer-facing guess that `权重` might also describe drag's authority. 19 merges applied, `PRAGMA foreign_key_check` clean. Graph: **1,017 concepts / 2,898 mentions / 49 relations**. Remaining 57 failures: 13 `PROVIDER_ITEM_ERROR`, 13 `EVIDENCE_ABSENT`, 12 alias holds (the deliberately declined groups), 10 `EVIDENCE_TOO_SHORT`, 6 `ANCHOR_MISMATCH`, 3 `EVIDENCE_AMBIGUOUS`. The re-poll reported `retained: 151` — items the newly enforced evidence floor re-rejects whose durable success stands, which is exactly the case the non-aborting retention was built for.
- **Section-graph v3 sample `965c2c11`: 15/16.** v3 took `EVIDENCE_ABSENT` from 3 to **0**, confirming the `toc_path` diagnosis. Four administrator merges then cleared the alias holds — `校验判定` ← `枢纽的校验判定`, `潮位站` ← `潮位观测站`, `北岸站` ← `北岸测站`, `南岸站` ← `南岸测站`, each one referent under a bare and a fuller form — after checking that none of the packets' relations would collapse into a self-loop. Those merges also unblocked 32 further items in the full run.
- **Section-graph gate cleared and the full pass is running (2026-08-07).** The last blocking item asserted `双轨校准法 --HAS_PART--> 观测规程2.4-2.11`, whose endpoints are one concept because an administrator merged the procedure with its citation — a consequence of a correct merge, not a model error. SDD 4.2.2 gained point 6: ingest skips and counts such a relation instead of failing the packet, matching what `merge_concepts` already does with a self-loop, while an endpoint naming an undefined `local_id` stays a hard failure. Sample `965c2c11` reached **16/16** with `skipped_self_relations = 1`, was approved, and full job **`31efbf3b`** (43 packets, `zh-section-graph-v3`, gpt-4.1, ~$0.89) is submitted. The approval record carries its prompt profile, so T-190's gate is now demonstrably four-coordinate on real data.
- **Latent bug found by the first re-poll after the evidence floor landed.** Refusing to demote a durable success was raised as a plain `BatchPayloadError` from inside the failure-recording handler, so it escaped `poll_and_ingest` and killed the whole pass — one already-succeeded item blocked every item after it, and the poll died before reaching the item it was run for. It is now `DurableSuccessRetained`, counted and reported as `retained` rather than raised. This deliberately changed behaviour a test asserted; the guarantee it protects (a later output can never replace a durable success) is unchanged and now asserted directly by comparing stored response bytes before and after.
- **`SECTION_GRAPH` full run is blocked and was not submitted.** The gate needs a sample whose job and every item succeeded; `fc38f176` is 12/16 and `3a939545` is 11/16, failing on 8 x `EVIDENCE_ABSENT` — the model paraphrasing instead of copying verbatim. Unlocking it needs a further section-graph profile iteration targeting paraphrase (the concept path solved the same failure class with an explicit verbatim-copy clause naming the normalizations to avoid) and a fresh ~$0.14 sample reaching 16/16. Do not shrink the sample to make the gate easier to satisfy; the gate exists to stop a full run proceeding on partial evidence.
- **Merges applied (T-160), all `PROVISIONAL` model-created concepts:** `二〇二〇年三月一日` ← `2020年3月1日` (same date, Chinese vs Arabic numerals); `扰动源` ← `强扰动源` (same entity, short vs fuller form); `双轨校准法` ← `《观测规程》2.4-2.11` (procedure kept as the concept, its citation retained as a lookup alias). Recorded in `concept_merges`. Graph: 69 concepts / 75 aliases / 133 mentions.
- **Known residual risk in v6:** the floor is unreachable on short passages — of the 20 sampled, eight are at most 10 code points and two are 9 — so its escape hatch makes `evidence` the entire passage there. That is a valid verified citation but a coarse one, and worth inspecting on the ingested mentions before a full-book run.
- **Diagnostics gap found while reading these results:** the alias-collision review hold persists no `failure_diagnostics_json`, so it shows as `(no diagnostics)` and is easy to misread as a prompt failure. It deserves its own reason slug alongside the grounding ones; recorded here rather than fixed, since it is a distinct change from the phase under review.
- **Cloud sample ledger (local E2E store `/private/tmp/aurapro-epub-e2e/epub_concept_v1.db`), all on version `b7e4d0ed`, 20 items each:**

  | Job | Model snapshot | Profile | State |
  |---|---|---|---|
  | `a99b731b` | gpt-4.1-2025-04-14 | v3 | 13/20 |
  | `31140302` | gpt-4o-mini-2024-07-18 | v3 | 17/20 |
  | `ec7fe945` | gpt-4.1-2025-04-14 | v4 | 19/20 |
  | `b893528d` | gpt-4o-mini-2024-07-18 | v4 | 15/20 |
  | `419dd120` | gpt-4.1-2025-04-14 | **v5** | 19/20 after merge review |
  | `e6e027d1` | gpt-4o-mini-2024-07-18 | **v5** | 17/20 |
  | `005a7a20` | gpt-4.1-2025-04-14 | **v6** | **20/20 — approvable** |
  | `82016085` | gpt-4o-mini-2024-07-18 | **v6** | 19/20 |
  | `fc38f176` | gpt-4.1-2025-04-14 | **section-graph v2** | SUBMITTED 2026-08-05, 16 packets |
  | `3a939545` | gpt-4o-mini-2024-07-18 | **section-graph v2** | SUBMITTED 2026-08-05, 16 packets |

  The two `SECTION_GRAPH` samples are the first relation-layer jobs ever run: all eight
  earlier jobs were `CONCEPT_MENTIONS`, so `concept_relations` was empty. They reuse the
  existing 69 concepts through deterministic normalized-name/alias matching rather than
  duplicating them. Estimated cost about $0.14 for the pair; the full book would be roughly
  $0.89 across 43 packets, cheaper per character than concept extraction because a packet
  amortizes the system instruction.

  **Read this table by grounding failures, not ingest count.** A "review-hold" item is not a
  prompt defect: it is `_resolve_or_create_concept` refusing an alias that matches several
  existing concepts and demanding administrator review, exactly as SDD 4.2 requires of
  model-suggested merges. Those holds are a function of accumulated graph state, which grew
  from 52 concepts / 54 aliases before these runs to 69 / 71 after, so later arms met more
  collisions than earlier ones. Ingest counts across arms are therefore confounded by
  execution order; grounding failures are not.

  Grounding failures per arm, which is the prompt-quality signal:

  | Model snapshot | v3 | v4 | v5 | v6 |
  |---|---|---|---|---|
  | gpt-4.1-2025-04-14 | 7 | 1 | 1 | **0** |
  | gpt-4o-mini-2024-07-18 | 3 | 5 | 3 | **1** |

  v6 is best or tied-best on both snapshots, and the improvement is monotone from v4. Both
  hypotheses held: `PROVIDER_ITEM_ERROR` truncations fell 6 → 1 → 0 as `max_tokens` went
  512 → 2048, and the minimum evidence span removed the remaining ambiguity failures.
  v6/gpt-4.1 met the grounding contract on all 20 items. The one v6 failure left is a
  `gpt-4o-mini` `EVIDENCE_ABSENT` paraphrase (30 code points, 0 occurrences, on the
  692-code-point passage) — not a truncation.

  The v5 and v6 samples were administrator-authorized on 2026-08-05 and run in parallel,
  since a Batch round trip costs hours and the two profiles test independent levers: v5
  raises `max_tokens` and makes anchors conditional, v6 adds the minimum evidence span on
  top of that. Deterministic stratified selection put every arm on the same 20 passages as
  the v4 samples (verified: 20 shared, 0 divergent in both), so all four comparisons are
  controlled. Submitted prompts verified at the wire: both carry the anchored schema at
  `max_tokens=2048`; only v6 carries the floor clause, the short-passage escape hatch, and
  the shape example at `end_codepoint:10`.

  Poll with `BatchJobService.recover_all`, which touches only `SUBMITTED`/`RUNNING` jobs
  and cannot create spend. Earlier pairs completed roughly an hour after submission against
  a 24h window. Pre-poll backups of the database are retained beside it under `backup-*/`.
- **Reusable local test asset:** Retain the isolated macOS Desktop runtime at `/private/tmp/aurapro-desktop-e2e` for later T-090 and E2E runs. It contains the Desktop-managed Python runtime, llama.cpp binary, and local test model, but no EPUB source material. Do not remove it before all planned local validation is complete; remind the administrator to reclaim the temporary disk space at final project completion.
